module.exports = {
  content: ["./src/**/*.{js,jsx,ts,tsx}"],
theme: {
    extend: {
      // Adds a new breakpoint in addition to the default breakpoints
      screens: {
        '3xl': '1600px',
      }
    }
  }
  plugins: [],
};
